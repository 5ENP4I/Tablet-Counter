<Window x:Class="TabletCounter.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TabletCounter"
        mc:Ignorable="d"
        Title="MainWindow" Height="675" Width="360">
    <Grid Background="#FFEFEFEF">
        <Grid Grid.Row="2">

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Button x:Name="Button1" Style="{StaticResource MenuButton}">
                <Image Source="data\checklist.png"/>
            </Button>
            <Button x:Name="Button2" Style="{StaticResource MenuButton}" Grid.Column="1">
                <Image Source="data\smallHouse.png"/>
            </Button>
            <Button x:Name="Button3" Style="{StaticResource MenuButton}" Grid.Column="2">
                <Image Source="data\settings.png"/>
            </Button>

        </Grid>

        <Label x:Name="Title" Content="Title" HorizontalContentAlignment="Stretch" VerticalContentAlignment="Center" Margin="5,5,5,5"/>

        <Grid.RowDefinitions>
            <RowDefinition Height="65*"/>
            <RowDefinition Height="610*"/>
            <RowDefinition Height="65*"/>
        </Grid.RowDefinitions>
    </Grid>
</Window>
